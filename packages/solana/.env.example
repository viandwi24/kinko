# Solana
SOLANA_RPC_URL=https://api.devnet.solana.com

# Operator keypair as JSON byte array — get with: cat <keypair.json> | tr -d '\n'
OPERATOR_PRIVATE_KEY=[1,2,3,...]

# Agent service URL (used in ERC-8004 registration doc)
AGENT_SERVICE_URL=http://localhost:3001
