{
  "name": "kinko",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "contract"
  ],
  "scripts": {
    "dev:web": "bun run --cwd packages/web dev",
    "dev:agent-a": "bun run --cwd apps/agent-a dev",
    "dev:agent-b": "bun run --cwd apps/agent-b dev",
    "build:web": "bun run --cwd packages/web build",
    "build:contract": "cd contract && anchor build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
